bind kb reset

# WASD style camera movement
bind kb w setc up
bind kb a setc left
bind kb s setc down
bind kb d setc right

# IJKL style camera zoom and rotate
bind kb j setc prev
bind kb l setc next
bind kb i setc closer
bind kb k setc farther

# Raise and Lower Selected Grid Tiles
bind kb up pick raise
bind kb down pick lower